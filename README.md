# 📦 سوغالری - سامانه مدیریت فروشگاه کودک

سامانه ظریف و فراندلی برای مدیریت محصولات، دسته‌بندی‌ها و تصاویر فروشگاه کودک.

## ویژگی‌ها

- ✅ **مدیریت دسته‌بندی‌ها**: اضافه و حذف دسته‌بندی‌ها به راحتی
- ✅ **مدیریت محصولات**: نام، قیمت، موجودی، سایز، رنگ و توضیحات
- ✅ **آپلود تصاویر**: آپلود مستقیم تصاویر محصولات
- ✅ **نگهداری لوکال**: ذخیره‌سازی بر روی LocalStorage
- ✅ **صفحه فارسی**: رابط سامانه با دعم کامل راست‌به‌چپ
- ✅ **GitHub و Google Drive**: همگام‌سازی خودکار
- ✅ **GitHub Actions**: در ویترلاینر خودکار

## راهنمای استفاده

### بر روی وب

1. فايل `index.html` را با مرورگر باز کنید
2. کرامی عمل مانع‌کننده CORS را غیرفعال کنید تا فایل‌ها به‌درستی بار شوند

### ارائه درخاست

```bash
python -m http.server 8000
# دسترسی: http://localhost:8000
```

### گذاشتن در روی سرور

1. فایل‌ها را با FTP/SFTP ارسال کنید
2. فایل‌ها: `index.html`, `styles.css`, `app.js`

## ساختار پروژه

```
sougallery/
├─ index.html          # صفحه اصلی HTML
├─ styles.css         # استایل‌های CSS
├─ app.js             # لوجیک JavaScript
├─ database.py        # مدیر پایگاه داده Python
├─ sync.sh            # اسکریپت همگام‌سازی
├─ .github/
│  └─ workflows/
│     └─ deploy.yml     # وركفلو GitHub Actions
└─ README.md           # راهنمای
```

## ساختار دادهباس

```json
{
  "categories": ["string"],
  "products": [
    {
      "id": "string",
      "category": "string",
      "name": "string",
      "price": 0,
      "stock": 0,
      "size": "string",
      "color": "string",
      "description": "string",
      "image": "base64_string",
      "createdAt": "ISO_DATE"
    }
  ],
  "images": {},
  "lastUpdate": "ISO_DATE"
}
```

## گکاربری نکات

### تاب "دسته‌بندی‌ها"
- نام دسته‌بندی را وارد کرده روی "اضافه کردن" کلیک کنید
- برای حذف، بر "حذف" کلیک کنید

### تاب "محصولات"
- دسته‌بندی را انتخاب کرده و فرم را جمع‌آوری دهید
- تضویر اختیاری است
- دکمه "افزودن محصول" را کلیک کنید

### تاب "همگام‌سازی"
- "ذخیره و آپلود" برای توليد JSON
- "دانلود JSON" برای بنسب داده‌ها

## الزاماتها

- مرورگر مدرن (Chrome, Firefox, Safari, Edge)
- JavaScript فعال
- LocalStorage دسترسی موردو فعال

## ارسال به GitHub

```bash
chmod +x sync.sh
./sync.sh
```

## ارسال به Google Drive

1. فایل JSON را در آن دراى آلبوم برابر، راست‌الراست‌کرده العنوانی: https://drive.google.com/drive/folders/1Aqsj6ThcsFyASKqAUygbLl98gPKl7ctn

2. از Google Drive API استفاده کرده و ارسال تابع اوتوماتیک تنظیم کنید

## کفايتی‌ها

- ریا کم و ذخیره
- بگر جاواسکریپت
- راست‌به‌چپ ناگزير بالا
- تک و را جار پرنده

## لایسنس

MIT